<!-- What changed and why. The subject line says the outcome; this says the
     reason. If it closes an issue, say `Closes #N` here: the keyword is read
     at merge time, so adding it afterwards does nothing. -->

## Evidence

<!-- Paste the output. Not "tests pass": the command and what it printed.

     If this carries a test, say how you proved the test can fail. Break the
     rule, watch it go red, restore, and paste the red. A check that cannot
     fail proves nothing, and that is the one thing this repo asks of every
     non-trivial change. -->

```

```
