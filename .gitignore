node_modules/
dist/
.env
*.log
.DS_Store
Screenshot*.png

# Design notes and delivery plans stay local. Some carry pricing, go to market
# and cloud internals, and the rest are drafts that should be read before they
# are published. This repository has a public remote, so the whole directory is
# ignored rather than named file by file.
docs/
